export enum Sex {
  Man = "Мужчина",
  Woman = "Женщина",
}

export enum Steps {
  One = "one",
  Two = "two",
  Three = "three",
}

export enum Endpoints {
  Main = "/",
  Form = "/create",
  Error = "/error",
}
